{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if ! git rev-parse --verify HEAD^ >/dev/null 2>&1; then exit 1; elif git diff --quiet HEAD^ HEAD -- .; then exit 0; else exit 1; fi",
  "functions": {
    "src/app/**/*.{js,ts,jsx,tsx}": {
      "maxDuration": 60
    }
  }
}
